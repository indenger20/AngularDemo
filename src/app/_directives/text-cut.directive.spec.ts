import { TextCutDirective } from './text-cut.directive';

describe('TextCutDirective', () => {
  it('should create an instance', () => {
    const directive = new TextCutDirective();
    expect(directive).toBeTruthy();
  });
});
